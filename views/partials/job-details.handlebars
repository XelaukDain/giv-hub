{{!-- <section class="card">
  <a href="/job/{{id}}">
    <header>{{title}}</header>

    <div class="card-body">
      <img src="/images/{{filename}}" alt="{{description}}">
      <p>By: {{technician}}</p>
    </div>
  </a>
</section> --}}


<div class="carousel-item">
 <img src="/images/{{filename}}" alt="{{description}}">
        <div class="container">
          <div class="carousel-caption">
            <h1>{{title}}</h1>
            <p>By: {{technician}}</p>
            <p><a class="btn btn-lg btn-warning" href="/job/{{id}}">Learn more</a></p>
          </div>
        </div>
      </div>